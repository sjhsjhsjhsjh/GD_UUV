# 终端监视器配置
monitor:
  # 启用/禁用监视器
  enabled: true
  
  # 监视器占据终端高度的比例（0.0 到 1.0）
  # 默认：0.67 表示占据终端下方的 2/3
  height_ratio: 0.67
  
  # 后台线程刷新间隔（毫秒）
  refresh_rate_ms: 100
  
  # 事件区保留的最大事件数
  max_events: 5
  
  # 最小高度（行数）
  min_height: 8

